# Sanitize a file name from ServCat metadata

Sanitize a file name from ServCat metadata

## Usage

``` r
sanitize_file_name(file_name, fallback)
```

## Arguments

- file_name:

  File name from the ServCat API.

- fallback:

  Fallback file name.

## Value

A length-1 character vector.
