# Simplify selected fields in a ServCat ReferenceProfile

Converts selected nested profile fields to simpler R objects while
preserving API field names.

## Usage

``` r
simplify_reference_profile(profile)
```

## Arguments

- profile:

  A named list representing a ServCat ReferenceProfile.

## Value

A modified ReferenceProfile list.
