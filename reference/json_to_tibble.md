# Convert JSON records to a tibble

Convert JSON records to a tibble

## Usage

``` r
json_to_tibble(x, rename_key = FALSE)
```

## Arguments

- x:

  Parsed JSON object from a ServCat response.

- rename_key:

  Logical. Rename a `key` column to `code`?

## Value

A tibble.
