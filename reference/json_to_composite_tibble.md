# Convert composite JSON records to a tibble

Convert composite JSON records to a tibble

## Usage

``` r
json_to_composite_tibble(x)
```

## Arguments

- x:

  Parsed JSON object from a ServCat composite endpoint.

## Value

A tibble. Nested objects are preserved as list-columns.
