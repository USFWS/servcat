% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get.R
\name{search_references}
\alias{search_references}
\title{Search ServCat references}
\usage{
search_references(
  criteria,
  top = 25,
  page = 1,
  orderby = NULL,
  sort = NULL,
  composite = FALSE,
  all_pages = FALSE,
  secure = FALSE,
  api_key = NULL
)
}
\arguments{
\item{criteria}{A named list of Advanced Search criteria. Supported top-level
fields include:

\describe{
\item{\code{quickSearch}}{A character string to search using ServCat Quick
Search. If supplied, Quick Search is the primary source of results, and
other filters are applied to those results.}
\item{\code{visibility}}{Optional visibility filter. Use \code{"public"} to return
only public records, \code{"internal"} to return only internal records, or
\code{NULL} to omit the visibility filter. Non-secure services can return
only public records.}
\item{\code{legacy}}{Optional legacy-status filter. Use \code{"excludelegacy"} to
return only non-legacy records, \code{"onlylegacy"} to return only legacy
records, or \code{NULL} to omit the legacy filter.}
\item{\code{version}}{Optional version filter. Use \code{"all"} to include all
versions of versioned records, or \code{NULL} to omit the version filter.}
\item{\code{regions}}{A list of Region filters. Each entry may include
\code{order}, \code{logicOperator}, and \code{unitCode}.}
\item{\code{units}}{A list of Unit filters. Each entry may include \code{order},
\code{logicOperator}, \code{unitCode}, \code{linked}, and \code{approved}.}
\item{\code{textFields}}{A list of text-field filters. Each entry may include
\code{order}, \code{logicOperator}, \code{fieldName}, and \code{searchText}. Valid
\code{fieldName} values include \code{"Abstract"}, \code{"Notes"},
\code{"TableOfContent"}, \code{"ContactName"}, \code{"Publisher"}, \code{"Keyword"},
\code{"MiscellaneousCode"}, \code{"Title"}, and \code{"DisplayCitation"}.}
\item{\code{dates}}{A list of date filters. Each entry may include \code{order},
\code{logicOperator}, \code{fieldName}, \code{filter}, \code{startDate}, and \code{endDate}.
Valid \code{fieldName} values include \code{"DateOfIssue"}, \code{"ContentBeginDate"},
\code{"ContentEndDate"}, \code{"LastEdited"}, and \code{"DateCreated"}. Valid
\code{filter} values include \code{"BeforeDate"}, \code{"AfterDate"},
\code{"BetweenDates"}, \code{"Exactly"}, \code{"NotEquals"}, and \code{"NotBetween"}.}
\item{\code{referenceTypes}}{A list of Reference Type filters. Each entry may
include \code{order}, \code{logicOperator}, and \code{referenceType}.}
\item{\code{referenceGroups}}{A list of Reference Type Group filters. Each
entry may include \code{order}, \code{logicOperator}, and \code{group}.}
\item{\code{rectangles}}{One or more bounding boxes represented as OGC
Well-Known Text polygon strings, for example
\code{"POLYGON((-121.8 45.7,-116.4 45.7,-116.4 42.0,-121.86 42.0,-121.8 45.7))"}.}
\item{\code{subjectCategories}}{A list of Subject Category filters. Each
entry may include \code{order}, \code{logicOperator}, and \code{subjectCategory}, where
\code{subjectCategory} is a numeric Subject Category identifier.}
\item{\code{digitalResources}}{A list of Files and Links filters. Each entry
may include \code{order}, \code{logicOperator}, \code{type}, \code{fieldName}, and
\code{searchText}. Valid \code{type} values include \code{"DigitalFile"},
\code{"ExternalLink"}, and \code{"WebService"}.}
\item{\code{physicalCopies}}{A list of Physical Copy filters. Each entry may
include \code{order}, \code{logicOperator}, \code{unitCode}, and \code{searchText}.}
\item{\code{collections}}{A list of Saved Collection filters. Each entry may
include \code{order}, \code{logicOperator}, and \code{collection}, where \code{collection}
is a numeric Saved Collection identifier.}
\item{\code{people}}{A list of Owner or Creator filters. Each entry may
include \code{order}, \code{logicOperator}, \code{fieldName}, and \code{searchText}. Valid
\code{fieldName} values include \code{"Creator"} and \code{"Owner"}. The \code{searchText}
value should be a staff UPN or partner user code.}
}

For criteria sections that accept multiple entries, \code{logicOperator} can be
used to combine or exclude criteria. Common values include \code{"AND"},
\code{"OR"}, and \code{"NOT"}. Group operators such as \code{"ANDGROUP"}, \code{"ORGROUP"},
and \code{"NOTGROUP"} may be used where supported by the ServCat API.}

\item{top}{Number of entries per page. Defaults to \code{25}. Use a larger
integer, such as \code{1000}, to reduce paging, but avoid values so large that
the request may time out.}

\item{page}{One-based page index to return. Defaults to \code{1}. If
\code{all_pages = TRUE}, this is the first page requested.}

\item{orderby}{Optional name of a single field by which to sort results.}

\item{sort}{Optional sort direction. Must be \code{"ASC"} or \code{"DESC"} if supplied.}

\item{composite}{Logical. If \code{TRUE}, use the composite Advanced Search
endpoint, which returns additional nested detail such as linked resources
and associated units. Defaults to \code{FALSE}.}

\item{all_pages}{Logical. If \code{TRUE}, request pages sequentially starting with
\code{page} and combine results into one tibble. Defaults to \code{FALSE}.}

\item{secure}{Logical. Use the secure API?}

\item{api_key}{Optional secure API key. If omitted, the package API-key
helper is used for secure requests.}
}
\value{
A tibble of Advanced Search result items. Page metadata from the
response is stored in the \code{"page_detail"} attribute. When
\code{all_pages = TRUE}, \code{"page_detail"} is a list containing page metadata for
each requested page. When \code{composite = TRUE}, the returned tibble may
include list-columns such as \code{linkedResources} and \code{units}.
}
\description{
Executes a ServCat Advanced Search using a POST request. Search criteria are
posted in the request body, while paging and sorting parameters are sent as
URI query parameters.
}
\details{
\code{NULL} values in \code{criteria} are recursively omitted before the JSON request
body is serialized.
}
\examples{
\dontrun{
# Quick Search using the Advanced Search endpoint
results <- search_references(
  criteria = list(
    quickSearch = "Kodiak, goats"
  )
)

results

# View paging metadata
attr(results, "page_detail")

# Request more results per page
results <- search_references(
  criteria = list(
    quickSearch = "Kodiak, goats"
  ),
  top = 100
)

# Retrieve all result pages
all_results <- search_references(
  criteria = list(
    quickSearch = "Kodiak, goats"
  ),
  top = 100,
  all_pages = TRUE
)

# Return composite results with linked resources and units
composite_results <- search_references(
  criteria = list(
    quickSearch = "Kodiak, goats"
  ),
  composite = TRUE
)

# Search public records and sort by issue date
results <- search_references(
  criteria = list(
    quickSearch = "Kodiak, goats",
    visibility = "public"
  ),
  top = 100,
  page = 1,
  orderby = "dateOfIssue",
  sort = "DESC"
)

# Search by reference type. NULL fields are omitted from the request body.
reports <- search_references(
  criteria = list(
    referenceTypes = list(
      list(
        order = 1,
        logicOperator = NULL,
        referenceType = "Unpublished Report"
      )
    )
  )
)

# Search within title text
title_results <- search_references(
  criteria = list(
    textFields = list(
      list(
        order = 1,
        logicOperator = NULL,
        fieldName = "Title",
        searchText = "mountain goat"
      )
    )
  )
)

# Search by date range
date_results <- search_references(
  criteria = list(
    dates = list(
      list(
        order = 1,
        logicOperator = NULL,
        fieldName = "DateOfIssue",
        filter = "BetweenDates",
        startDate = "2010-01-01",
        endDate = "2020-12-31"
      )
    )
  )
)

# Search for records with public digital files
file_results <- search_references(
  criteria = list(
    digitalResources = list(
      list(
        order = 1,
        logicOperator = NULL,
        type = "DigitalFile",
        fieldName = NULL,
        searchText = NULL
      )
    )
  )
)
}

}
